name: Continuous Integration for Flutter applications

on:
  push:
    branches:
      - "main"

jobs:
  test:
    uses: fermi-ad/.github/.github/workflows/flutter-integration.yaml@02de88f39fb7a7dd994414898abaef606153fb5a
    secrets: inherit
    strategy:
      fail-fast: false
      matrix:
        test: 
          - smoke_test
