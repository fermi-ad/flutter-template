name: Continuous Integration for Flutter applications

on:
  push:
    branches:
      - "main"

jobs:
  test:
    uses: fermi-ad/.github/.github/workflows/flutter-integration.yaml@7d2f4e35d73af6e66228045945cb546719ab066f
    secrets: inherit
    strategy:
      fail-fast: false
      matrix:
        test: 
          - smoke_test
